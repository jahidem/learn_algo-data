#include <bits/stdc++.h>
using namespace std;
using ll = long long;
vector<int> ls;
int dp[10001][2];
int dpx(int idx,int f){
  int mx=0;
  
}
int main()
{
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
   //freopen("inp", "r", stdin);
  ll T = 1; // cin>>T;
  for(int tt=1;tt<=T;tt++){ 
    int n,k;cin>>n>>k;
    ls = vector<int> (k);
    for(int &x:ls)
      cin>>x;
    
}
